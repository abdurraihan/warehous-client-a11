import React from 'react';

const Blog = () => {
    return (
        <div>
            <h2>Difference between Javascript and nodejs ?</h2>
            <p>javascript in a programminr language and nodeJs in runtime</p>
            <h2> When you should nodejs and when you should mongodb?</h2>
            <p>when I need work with database then i will use mongobd and when I work with backend by js then I use nodejs</p>
            <h2>Diffrence between sql and nosql database ??</h2>
            <p>1. SQL databases use structured query language and have a predefined schema. NoSQL databases have dynamic schemas for unstructured data.
                2. SQL databases are relational, NoSQL databases are non-relational.
                2. SQL databases are vertically scalable, while NoSQL databases are horizontally scalable.</p>
        </div>
    );
};

export default Blog;